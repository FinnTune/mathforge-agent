# Runtime dependencies only (mirrors [project.dependencies] in pyproject.toml).
# For an editable dev install with pytest + ruff:
#   pip install -e ".[dev]"
#
# Plain pip users without pyproject workflows can use:
#   pip install -r requirements.txt

langgraph==1.1.8
langchain-anthropic==1.4.1
langchain-core==1.3.0
python-dotenv==1.2.2
discord.py>=2.4,<3
numpy>=2.0,<3
sympy>=1.13,<2
matplotlib>=3.9,<4
scipy>=1.14,<2
