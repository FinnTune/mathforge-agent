# Runtime dependencies only (mirrors [project.dependencies] in pyproject.toml).
# For an editable dev install with pytest + ruff:
#   pip install -e ".[dev]"
#
# Plain pip users without pyproject workflows can use:
#   pip install -r requirements.txt
#
# The scientific stack (numpy/sympy/matplotlib/scipy) is no longer a dependency
# here — it's only needed by the Python interpreter the Rust sandbox server
# invokes (MATHFORGE_SANDBOX_PYTHON); see mcp-servers/sandbox-rs/requirements.txt.

langgraph==1.1.8
langchain-anthropic==1.4.1
langchain-core>=1.6.2,<2
python-dotenv==1.2.2
discord.py>=2.4,<3
langchain-mcp-adapters==0.3.2
